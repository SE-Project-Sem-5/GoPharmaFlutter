import 'package:flutter/material.dart';

class DeliveryFullView extends StatelessWidget {
  const DeliveryFullView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
